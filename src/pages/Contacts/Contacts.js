import React, { Component } from "react";
import Layout from "../../components/Layout/Layout";

function Contacts() {
    return (
        <Layout>
            <h1>Contacts page</h1>
        </Layout>
    );
}

export default Contacts;